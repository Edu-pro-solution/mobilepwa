import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eduprosolution.app',
  appName: 'Edu Pro Solution',
  webDir: 'build'
};

export default config;
